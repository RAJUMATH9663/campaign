{
  "name": "campaign-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"server,worker,client\" -c \"blue,magenta,green\" \"npm --prefix server run dev\" \"npm --prefix server run worker\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:worker": "npm --prefix server run worker",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
